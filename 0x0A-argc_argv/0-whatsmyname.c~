#include <main.h>
#include <stdio.h>

/**
 * main -  function
 * @argc : argument count
 * @argv : argument value
 * Return : Always 0
 */

int main(int argc, char *argv[])
{
if (argc > 0)
printf("%s\n", argc[0]);

return (0);
}
