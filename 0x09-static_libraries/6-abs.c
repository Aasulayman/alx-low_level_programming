#include "main.h"
/**
 * _abs - check function that computes the absolute value
 * @a : parameter to check the absvalue
 * Return: Always 0.
 */
int _abs(int a)
{
if (a < 0)
{
return (-1 * a);
}
else
{
return (a);
}
}
