#include "main.h"

/**
 * puts2 - check the code to print every character of a string
 * @str : parameter to print 
 * Return: Always 0.
 */
void puts2(char *str)
{
for (int = 0; i < str[i]; i += 2)
{
_putchar(str[i]);
}
}
