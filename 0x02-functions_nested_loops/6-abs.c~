#include "main.h"
/**
 * int _abs(int) - checkfu nction that computes the absolute value
 * @a - parameter to check the absvalue
 * Return: Always 0.
 */
int _abs(int a);
{
if (a < 0)
return (-a);
else
return (a);
}
