#include "main.h"
#include <stdio.h>

/**
 * is_palindrome - check code to return a string
 * @s : parameter for string
 * @n : palindrome
 * Return: Always 0.
 */
int is_palindrome(char *s)
{
char n
if (n == *s)
{
return (1);
return (is_palindrome(n);
}
return (0);  
}
